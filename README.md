# STEP Volume Calculator

A simple Streamlit web app to calculate volume from STEP (.step/.stp) files.

## Features
- Upload STEP file
- Calculates total volume
- Supports multiple solids
- Unit conversion (mm³, cm³, liters, m³)

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
